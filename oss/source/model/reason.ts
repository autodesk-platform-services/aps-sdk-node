

/**
 * An object returned with an error describing the reason for failure.
 * @export
 * @interface Reason
 */
export interface Reason {
    /**
     * reason for failure
     * @type {string}
     * @memberof Reason
     */
    'reason': string;
}

